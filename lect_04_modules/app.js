 const sum = (a, b) => {
  console.log(`sum of a ${a} and b ${b} is = ${a + b}`);
};
 const subTract = (a, b) => {
  console.log(`subtraction of a ${a} and b ${b} is = ${a - b}`);
};

export {sum, subTract}